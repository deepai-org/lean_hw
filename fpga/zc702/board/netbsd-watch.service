[Unit]
Description=Watch the fabric guest and recover it if it stops answering
[Service]
Type=oneshot
Environment=LOOM_BOARD_ROOT=%h/substrate0
EnvironmentFile=-%h/.config/loom-board.env
ExecStart=/usr/bin/env bash -c 'exec "$LOOM_BOARD_ROOT/test/netbsd_watch.sh"'
